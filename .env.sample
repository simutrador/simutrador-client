# SimuTrador Client - Environment Sample
# Copy this file to `.env` and adjust values as needed.
# You can also set ENV=/absolute/path/to/.env to point to a different file.

# --- Server/WebSocket configuration ---
# Base WebSocket server URL (scheme://host:port) used by the CLI when --url is not provided
SERVER__WEBSOCKET__URL=ws://127.0.0.1:8000

# --- Authentication (placeholders for future use) ---
# When authentication is implemented, populate one of the following as appropriate.
# AUTH__TOKEN=
# AUTH__API_KEY=

# Notes:
# - Nested settings use double underscores (__) as separators.
# - CLI precedence: command-line arguments > environment variables / .env > built-in defaults.

